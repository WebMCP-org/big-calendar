# Big Calendar currently does not require any local `.env` variables.
#
# Keep this file so contributors have a single place to document future
# environment configuration if the demo adds backend or hosted-client setup.
